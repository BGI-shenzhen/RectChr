
SetParaFor=global             	##设置全局范围(global)相关变量

File1=./Value.data.gz                   	##输入第1个File的路径
track_num=9                             	##总绘图层数(track_num)为9,默认会自动推断
chr_order=Chr02                         	##指定只画这条Chr02染色体
#chr_spacing_ratio=1.5                  	##不同染色体之间的间隔比例（基于Sum(track_height)计算）

SetParaFor=trackALL           	##设置所有Track的相关参数

plot_type=PairWiseLink                  	##为trackALL设置绘图方式PairWiseLink(彩虹链接)
track_height=20                         	##当前层(track)的高度（即轨道高度）
#padding_ratio=0.1                      	##同一染色体内相邻track的垂直间距比例（track_height*padding_ratio）
colormap_legend_show=0                  	##不显该trackALL的图例
colormap_legend_shift_x=-40             	##控制图例渐变在水平方向上的位置
#chr_label_shift_x=0/ShiftChrNameY      	##染色体标签的横向偏移量
chr_label_size_ratio=0.01               	##染色体标签字体大小比例(相对于默认值)
padding_ratio=0.05                      	##同一染色体内相邻track的垂直间距比例（track_height*padding_ratio）

SetParaFor=track1             	##开始设置第1个Track的相关参数

track_height=10                         	##该track1的height调低些
plot_type=EEE                           	##非关健词,即此track仅画背色

SetParaFor=track2             	##开始设置第2个Track的相关参数

plot_type=PairWiseLink                  	##为track2设置绘图方式PairWiseLink(彩虹链接)
background_color="#F4F4FF"              	##背景颜色
track_height=20                         	##当前层(track)的高度（即轨道高度）
stroke-width=0.5                        	##控制绘图中线条的粗细程度
show_columns=File1:4                    	##确定绘图中需要显示的列，所在文件所在列
colormap_legend_show=1                  	##决定图例中的渐变效果是否显示
#link_linestyle=curve                   	##用直线还是贝塞尔曲线
#link_uniform_height=1                  	##Link中是否统一高度
#link_direction=UpUp                    	##Link绘图中线条起点和终点的方向

SetParaFor=track3             	##开始设置第3个Track的相关参数

plot_type=PairWiseLink                  	##为track3设置绘图方式PairWiseLink(彩虹链接)
background_color="#F4F4FF"              	##背景颜色
track_height=20                         	##当前层(track)的高度（即轨道高度）
show_columns=File1:4                    	##确定绘图中需要显示的列，所在文件所在列
link_linestyle=line                     	##用直线还是贝塞尔曲线
link_direction=UpDown                   	##Link绘图中线条起点和终点的方向

SetParaFor=track4             	##开始设置第4个Track的相关参数

plot_type=PairWiseLink                  	##为track4设置绘图方式PairWiseLink(彩虹链接)
background_color="#F4F4FF"              	##背景颜色
track_height=20                         	##当前层(track)的高度（即轨道高度）
show_columns=File1:4                    	##确定绘图中需要显示的列，所在文件所在列
link_linestyle=line                     	##用直线还是贝塞尔曲线
link_direction=DownUp                   	##Link绘图中线条起点和终点的方向

SetParaFor=track5             	##开始设置第5个Track的相关参数

plot_type=PairWiseLink                  	##为track5设置绘图方式PairWiseLink(彩虹链接)
background_color="#F4F4FF"              	##背景颜色
track_height=20                         	##当前层(track)的高度（即轨道高度）
show_columns=File1:4                    	##确定绘图中需要显示的列，所在文件所在列
link_direction=DownUp                   	##Link绘图中线条起点和终点的方向

SetParaFor=track6             	##开始设置第6个Track的相关参数

plot_type=PairWiseLink                  	##为track6设置绘图方式PairWiseLink(彩虹链接)
background_color="#F4F4FF"              	##背景颜色
track_height=20                         	##当前层(track)的高度（即轨道高度）
show_columns=File1:4                    	##确定绘图中需要显示的列，所在文件所在列
link_direction=DownUpV2                 	##Link绘图中线条起点和终点的方向

SetParaFor=track7             	##开始设置第7个Track的相关参数

plot_type=PairWiseLink                  	##为track7设置绘图方式PairWiseLink(彩虹链接)
background_color="#F4F4FF"              	##背景颜色
track_height=20                         	##当前层(track)的高度（即轨道高度）
show_columns=File1:4                    	##确定绘图中需要显示的列，所在文件所在列
link_direction=UpDown                   	##Link绘图中线条起点和终点的方向

SetParaFor=track8             	##开始设置第8个Track的相关参数

plot_type=PairWiseLink                  	##为track8设置绘图方式PairWiseLink(彩虹链接)
background_color="#F4F4FF"              	##背景颜色
track_height=20                         	##当前层(track)的高度（即轨道高度）
show_columns=File1:4                    	##确定绘图中需要显示的列，所在文件所在列
link_direction=UpDownV2                 	##Link绘图中线条起点和终点的方向

SetParaFor=track9             	##开始设置第9个Track的相关参数

plot_type=PairWiseLink                  	##为track9设置绘图方式PairWiseLink(彩虹链接)
background_color="#F4F4FF"              	##背景颜色
track_height=20                         	##当前层(track)的高度（即轨道高度）
show_columns=File1:4                    	##确定绘图中需要显示的列，所在文件所在列
link_direction=DownDown                 	##Link绘图中线条起点和终点的方向
link_uniform_height=1                   	##Link中是否统一高度

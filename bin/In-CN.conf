##################################### 全局参数 #######################################################

SetParaFor = global              ## 设置当前段落作用域为全局范围（global），适用于整个图形配置

File1 = ./InPut.file             ## 主数据文件路径，必须输入。格式为 [Chr Start End Value1 ...]，其中 NA 表示不绘制该区域。
#File2 =                         ## 可选的第二数据文件路径，用于多源数据叠加  FileX = ./InPut.fileX

#track_num =                     ## 指定绘图层数（track 数量），默认根据 File1 中的列数自动推断（如 Value1 到 ValueN）
#chr_spacing_ratio = 0.2         ## 不同染色体之间的间隔比例，基于 track 高度计算（track_height * chr_spacing_ratio）
#title = "main_Figure"           ## 图形标题及其样式设置，支持 title_color、title_size、title_shift_x、title_shift_y 等参数
#colormap_conf = col.file        ## 自定义颜色映射文件路径，用于指定 value 到 color 的映射关系（例如 P1 = "#FE0808"）
#chr_orientation = horizontal    ## 染色体排列方向，可选值为 horizontal（横向）或 vertical（纵向）
#bg_end_arc = 1                  ## 较少使用的参数，用于控制背景弧线等特殊效果


######################################## global.染色体参数配置 ####################################################
#chr_zoom_region =               ## 缩放特定区域，格式为 chr:start:end（例如 chr2:1000:5000）
#chr_order =                     ## 指定染色体顺序或筛选显示的染色体列表（未指定则按名称自动排序）
#chr_spacing_ratio = 0.2         ## 不同染色体之间的间隔比例（基于 Sum(track_height) 计算）
#chr_label_rotation = 0          ## 染色体标签文字旋转角度



################################## global.画布 和 图片 参数配置 #################################

#canvas_body = 1200              ## 主画布大小，默认值为 1200 像素
#canvas_margin_top = 55          ## 画布上边距，默认为 55 像素
#canvas_margin_bottom = 25       ## 画布下边距，默认为 25 像素
#canvas_margin_left = 100        ## 画布左边距，默认为 100 像素
#canvas_margin_right = 120       ## 画布右边距，默认为 120 像素
#canvas_angle = 0                ## 整个图形旋转角度，单位为度


#################################  ALL 默认各层的配置参数 若各层没有配置的会，则会用这儿的参数 #########
SetParaFor = trackALL            ## 设置所有 Track 的默认参数，后续未单独配置的 trackX 将继承这些设置

plot_type = heatmap              ## 支持的绘图类型有：heatmap（热力图），line（折线图），scatter（散点图），histogram（直方图），LinkS（连接线）
                                 ##line, scatter/point, histogram，link,LinkS heatmap(highlights),text,PairWiseLink
                                 ##PairWiseLinkV2,heatmapAnimated/histAnimated,LinkS,shape和ridgeline

#show_columns =                  ## 指定要显示的列，如 File1:4 或 File2:4,5
#colormap_brewer_name =          ## 使用预设调色板（覆盖手动设置的颜色），例如 GnYlRd（数值）或 Paired（文本）
#colormap_reverse = 0            ## 是否反转颜色渐变条（0=正常，1=反转）
#colormap_low_color = "#006400"  ## 最低值对应的颜色
#colormap_mid_color = "#FFFF00"  ## 中间值对应的颜色
#colormap_high_color = "#FF0000" ## 最高值对应的颜色
#background_color = "#B8B8B8"    ## 背景颜色
#upper_outlier_ratio = 0.95      ## 定义顶部异常值阈值，高于此比例的值使用最高色
#lower_outlier_ratio = 0         ## 定义底部异常值阈值，低于此比例的值使用最低色
#Ymax =                          ## 手动设置当前层的最大值，覆盖自动计算结果
#Ymin =                          ## 手动设置当前层的最小值，覆盖自动计算结果
#cap_max_value =                 ## 限制最大值上限
#cap_min_value =                 ## 限制最小值下限
#colormap_nlevels = 8            ## 颜色渐变等级数量
#track_height = 20               ## 当前层的高度（即轨道高度）
#track_bg_height_ratio = 1       ## 背景高度占 track_height 的比例（0-1]
#log_p = 0                       ## 是否对数值进行 log10 转换（0=否，1=是）
#padding_ratio = 0               ## 同一染色体内相邻 track 的垂直间距比例（track_height * padding_ratio）
#colormap_legend_sizeratio =          ## 渐变条尺寸（宽度和高度）
#yaxis_tick_show = 0             ## 是否显示 Y 轴刻度标签（0=隐藏，1=显示）
#colormap_legend_show = 1        ## 是否显示颜色渐变条（0=隐藏，1=显示）
#colormap_legend_shift_x = 0     ## 渐变条水平偏移量
#colormap_legend_shift_y = 0     ## 渐变条垂直偏移量
#chr_label_shift_x = 0           ## 染色体标签的横向偏移量
#chr_label_shift_y = 0           ## 染色体标签的纵向偏移量
#chr_label_size_ratio = 1.0      ## 染色体标签字体大小比例（相对于默认值）
#track_shift_x = 0               ## track 层移动X值坐标
#track_shift_y = 0               ## track 层移动Y值坐标

##################################### trackALL. 其它不常见的参数 #######################################################

#text-font-size =                ## 文本字体大小设置
#track_text_size = 1.0           ## 文本字体大小比例（相对于默认值）
#...                             ### 更多参数

##################################### trackX 各层的参数，继承trackALL所有参数 #######################################################

#SetParaFor = track2             ## 开始处理第 2 层参数,要从1开始
#File2 =                         ## 可指定其他输入文件作为数据源
#plot_type = hist                ## 绘图类型：histogram（直方图）
#show_columns = File2:5          ## 显示 File2 的第五列数据（可用于散点图或其他图表类型）
#label = "Name"                  ## 当前层的标签及其样式设置（label_size label_color label_shift_x label_shift_y label_angle）

#SetParaFor = track3
#plot_type = lines               ## 绘图类型：折线图（lines）
#show_columns = File1:5,6        ## 显示 File1 的第 5 和第 6 列数据